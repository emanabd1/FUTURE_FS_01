import {useState} from 'react';import Icon from './Icon';import {profile} from '../lib/data';
export default function Navbar(){const[o,setO]=useState(false);const go=id=>{document.getElementById(id)?.scrollIntoView({behavior:'smooth'});setO(false)};return <header className="navbar"><div className="container nav"><button className="logo" onClick={()=>go('home')}><b>&lt;/&gt;</b> Portfolio</button><nav className={o?'open':''}>{[['Home','home'],['About','about'],['Work','work'],['Skills','skills'],['Contact','contact']].map(([x,id])=><button key={id} onClick={()=>go(id)}>{x}</button>)}</nav><div className="nav-actions"><a href={profile.github} target="_blank" rel="noreferrer"><Icon name="github"/></a><a href={profile.linkedin} target="_blank" rel="noreferrer"><Icon name="linkedin"/></a><a href={`mailto:${profile.email}`}><Icon name="mail"/></a><button className="menu" onClick={()=>setO(!o)}><Icon name={o?'close':'menu'}/></button></div></div></header>}
