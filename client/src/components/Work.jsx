import {useState} from 'react';import Icon from './Icon';import {projects,certificates,uiux} from '../lib/data';
export default function Work(){const[t,setT]=useState('projects');return <section id="work" className="section"><div className="container"><div className="heading workhead"><div><small>PORTFOLIO</small><h2>Selected <span>work.</span></h2></div><p>A collection of development projects, certificates and design work.</p></div><div className="tabs">{[['projects','Projects'],['certificates','Certificates'],['uiux','UI/UX Work']].map(([id,l])=><button className={t===id?'active':''} onClick={()=>setT(id)} key={id}>{l}</button>)}</div>{t==='projects'&&<div className="cards">{projects.map((p,i)=><article className="project" key={p.title}><div className="preview"><span>{String(i+1).padStart(2,'0')}</span><b>&lt;project /&gt;</b><em>{p.category}</em></div><div className="content"><div className="title"><h3>{p.title}</h3><a href={p.github} target="_blank" rel="noreferrer"><Icon name="arrow"/></a></div><p>{p.description}</p><div className="tags">{p.tech.map(x=><span key={x}>{x}</span>)}</div><a className="code" href={p.github} target="_blank" rel="noreferrer"><Icon name="github" size={16}/> Code</a></div></article>)}</div>}{t==='certificates'&&<div className="cards">{certificates.map(c=><article className="certificate" key={c.title}><div className="cert-img"><img src={c.image} alt={c.title}/></div><div className="content"><small>{c.issuer}</small><h3>{c.title}</h3><p>Certificate and training achievement.</p></div></article>)}</div>}{t==='uiux'&&<div className="cards">{uiux.map((u,i)=><article className="uiux" key={u.title}><div className="wire"><b>0{i+1}</b><i/><i/><i/></div><div className="content"><small>{u.tool}</small><h3>{u.title}</h3><p>Design work focused on user experience, interface structure and visual design.</p></div></article>)}</div>}</div></section>}
