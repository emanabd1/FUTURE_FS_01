import Icon from './Icon';import {skills} from '../lib/data';const icons={Frontend:'code',Backend:'database',Design:'palette',AI:'sparkle'};export default function Skills(){return <section id="skills" className="section"><div className="container"><div className="heading center"><small>TECHNOLOGIES</small><h2>Tools I <span>work with.</span></h2><p>Technologies and tools I use to turn ideas into working products.</p></div><div className="skill-grid">{Object.entries(skills).map(([k,v])=><article className="skill" key={k}><Icon name={icons[k]} size={28}/><h3>{k}</h3><div className="tags">{v.map(x=><span key={x}>{x}</span>)}</div></article>)}</div></div></section>}
